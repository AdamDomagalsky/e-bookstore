# Front-end library repository

## Dependencies
npm install

### Building
npm start - it will start webpack server on port 3000
npm test - it will run all tests
npm build - it will build production form of application
